<p id="notice"><%= notice %></p>

<h1><%= @crunch_algorithm.name %></h1>

<p>
  <strong>Type:</strong>
    <%= @crunch_algorithm.category %>
</p>



<p>
  <strong>Functions:</strong>
    <div style="margin-left: 2em">
      <% @crunch_algorithm.functions.each do |f| %>
        <b><%= f[:name] %></b> ( <%= [f[:args]].flatten.join(', ') %> )<br>
      <% end %>
    </div>
</p>



<%= link_to 'Edit Settings', edit_crunch_algorithm_path(@crunch_algorithm) %> |
<%= link_to 'Crunch Algorithms Index', crunch_algorithms_path %>
