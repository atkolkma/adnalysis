<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @crunch_algorithm.name %>
</p>

<p>
  <strong>Functions:</strong>
    <div style="margin-left: 3em">
      <% @crunch_algorithm.functions.each do |f| %>
        <b><%= f[:name] %></b><br>
        <b>arguments:</b> <%= f[:args] %><br><br>
      <% end %>
    </div>
</p>

<p>
  <strong>Type:</strong>
  <%= @crunch_algorithm.category %>
</p>


<%= link_to 'Edit', edit_crunch_algorithm_path(@crunch_algorithm) %> |
<%= link_to 'Back', crunch_algorithms_path %>
