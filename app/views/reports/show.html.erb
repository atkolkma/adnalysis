<p id="notice"><%= notice %></p>
<h1><%= @report.name %></h1>
<h3>From files: <%=  @report.source_files ? @report.source_files.join(', ') : "none"%></h3>
<%= link_to 'crunch', crunch_path(@report) %> |
<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>
